services:
  main:
    container_name: main
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .dev.env
    ports:
      - 5000:5000
    volumes:
      - .:/app
      - /app/node_modules
    command: npm run start:dev
    depends_on:
      - postgres
      - redis
    restart: always

  redis:
    container_name: redis_db
    image: 'redis:alpine'
    ports:
      - 6379:6379
  postgres:
    container_name: deathmatch_db
    image: postgres:15.2
    env_file:
      - .dev.env
    environment:
      PG_DATA: /var/lib/postgresql/data
    ports:
      - 5432:5432
    volumes:
      - pgdata:/var/lib/postgresql/data
    restart: always
  pgadmin:
    container_name: pg_admin
    image: dpage/pgadmin4
    env_file:
     - .dev.env
    ports:
      - 5050:80
volumes:
  pgdata: